# ECSExample
A simple fully self contained AWS ECS Example

This repository contains everything you need to set up the basic ECS infrastructure using cloudformation.

Once this is done, you should be able to deploy containers to it
